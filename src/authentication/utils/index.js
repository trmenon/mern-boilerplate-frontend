import { 
    setCookie,
    removeCookie, 
    getCookie 
} from './cookie';

import {
    setLocalStorage,
    removeLocalStorage
} from './localStorage';

export {
    setCookie,
    removeCookie, 
    getCookie,
    setLocalStorage,
    removeLocalStorage 
};